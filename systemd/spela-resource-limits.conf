[Service]
# Bound the media worker blast radius. WebTorrent and ffmpeg inherit these
# limits from spela.service, but the values still leave room for one large
# stream plus NVENC transcoding.
MemoryHigh=6G
MemoryMax=8G
MemorySwapMax=1G
TasksMax=256
CPUQuota=300%
