# Spela TODOs 🎬🍿

### 🔓 OPEN — the ranker has no idea what the VIEWER'S LINK can carry (2026-09-11)
Ranking is resolution → codec (target-scoped) → seeds. Nothing expresses **bytes per
second available to the person watching**, so the same #1 pick is served to a Chromecast
on gigabit LAN and to VLC on a phone hotspot two hundred km away over WireGuard. On
2026-09-11 that meant a 36.8 Mbps remux over a hotspot: unwatchable, and nothing in the
ranking could have known.

The bitrate is already derivable at rank time — parsed size ÷ TMDB runtime, no probe
needed. What is missing is the OTHER half: what the requester can actually sustain.
Options, cheapest first:
- **Declared**, per target: a `max_bitrate_kbps` on the play/search request, set once in
  the SPA's target picker (LAN / VPN / hotspot). Deterministic, no measurement, and the
  viewer knows their own link better than a probe would in the first 10 s.
- **Observed**: derive it from the last stream's delivered throughput to that client.
  Accurate, but arrives too late for the first pick of an evening.
- **Inferred**: the requester's address (LAN subnet vs a WireGuard peer) as a coarse prior.
  Free, but a hotspot and a fibre line look identical from behind the tunnel.

Leaning declared-with-an-observed-correction. **Decide the UX first** (does the target
picker grow a link setting, or does it live somewhere else?) — that is Fredrik's call, not
an implementation detail. Until then the Local-Bypass tie-break shipped 2026-09-11 stops
the worst case (being handed a remux when a small copy of the same film is on disk), but
does nothing when the only copies are large.

### 🔓 OPEN — a 1.2 GB partial download vanished across a restart (2026-09-09)
Star City S01E03 reached 1.2 GB and `open_pct 100`; after a `systemctl restart` it
was absent from the persistence store, its directory was gone, and a re-tap started
from 27 MB. **The pruner is exonerated by its own silence** (no eviction line) and
the boot reconciler by its own log (it recorded forgetting only a different,
genuinely empty torrent). Not ruled out: `race_torrent_sources` losing-candidate
cleanup, and `stop_torrent(.., delete_files = true)` on a failed start. The
reconciler was made incapable of deleting files the same evening — not because it
was proven guilty, but because removing the capability is cheaper than defending it.
**Next action**: instrument every remaining path that can delete a media file with a
log line naming what it deletes and why, then wait for a recurrence. Cost of being
wrong is bounded (a re-downloadable partial, never the curated library), which is
why persistence stays on.

### 📌 NOTE — the subtitle warm logs once per readiness poll while waiting
`no English subtitle found yet for <imdb>_eng` appears every ~1.5 s for the whole
buffering window (observed 2026-09-14: a solid two-minute run in the journal). It
is BOUNDED — it stops when the wait does, and `sub_missing` already negative-caches
the fetch itself — so this is log noise, not a leak or repeated work. Worth one
line of suppression the next time `handle_vlc_ready` is open; not worth a trip of
its own.

### 🔓 OPEN — unpin the librqbit fork when the upstream PR merges
`Cargo.toml` points at `semikolon/rqbit` by commit for one fix: the incoming-listener
starvation (ikatson/rqbit#663). **Next action**: watch that PR; when it lands in a
release, move back to crates.io and delete the fork note in `OPERATIONS.md`.

### 📌 NOTE — one MERIAN sync printed no trigger lines; not reproducing
The 2026-09-05 09:24 sync deployed 27 templates and reported `ok` with no
`nit: trigger '…' succeeded` lines at all. The 11:2x sync printed three,
including the new `42-restart-spela-vlc-bridge`. Unexplained; nit's own logic
counts a new trigger as changed, so it should have fired the first time. Watch
for a recurrence rather than chasing it — a bridge change that appears not to
take effect is the symptom, and the manual fallback is
`bash ~/dotfiles/scripts/darwin/42-restart-spela-vlc-bridge.sh`.

### 🔓 OPEN — see a next-episode hand-off end to end

Next-episode enqueue into the running VLC is shipped and the serving half is
verified; what has never been watched is an actual hand-off between two episodes,
because it is LOCAL-ONLY and the obvious test episode was a sparse placeholder.
**Next action**: download E02 first, then play E01 to the watched mark and watch it
roll over. Detail: CLAUDE.md Hard-Won "Next-episode ENQUEUE".

### Open — recommender + ledger follow-ups (2026-08-27)
- **Rating — SHIPPED 2026-08-27.** `WatchedEntry.rating: Option<i8>` (`1` loved / `0` fine / `-1` no; `None` = UNRATED and MUST stay distinguishable from "fine"). `POST /watched-rate`; rating a SHOW applies to all its episode rows and a re-mark carries the judgement across. Rewatch renders TWO toggles, never a cycle, plus a Loved-only filter.
- **imdb_id backfill — DONE 2026-08-27.** `POST /watched-backfill-ids` resolves ONE id per distinct title via `title_meta` with `auto_kind`, applies it to every row, idempotent — re-run it whenever the ledger grows. Ran clean: 107/107 rows carry a well-formed id.
- **`media_type=auto` is opt-in**; the To-Watch and rec rows still pass an explicit hint because they know it. Do not flip the default — OMITTING the hint has always meant "movie" and existing callers rely on it.

### Netflix viewing-activity import — OPEN, blocked on Fredrik (2026-08-26)
The structural cure for the lifetime-history gap (see § Watch tracker for why it matters — it produced five already-seen recommendations in one session). **Blocked**: the Bitwarden vault is logged OUT (`status: unauthenticated`, `lastSync: null`), so the Netflix credentials cannot be looked up. **Fredrik runs `bw login` then `bwu`** (master password + 2FA, human-only); then Claude searches the vault, pulls the PER-PROFILE CSV from `netflix.com/viewingactivity` (never the account-level GDPR export — it pulls every family profile), and builds the importer.

### 4K / partial-torrent VLC "buffer then AUTO-play" (2026-08-03)
**✅ MOSTLY SHIPPED (v3.21):** `TorrentEngine::prefetch_ends` (first+last-piece prefetch) + `GET /vlc/{id}/ready` + SPA `vlcWaitReady` (poll readiness, auto-fire `vlc://` when buffered, no re-click) are live. **Remaining refinements:** (a) ✅ SHIPPED (v3.22 `ends_present` — a short-timeout head+tail present-check, ANDed with the `≥15% + ≥64MB` proxy in `/vlc/ready`); (b) **rate-vs-bitrate gate — now the TOP open item, and the missing input exists as of 2026-09-06.** The gate is a fixed FRACTION (`MIN_FRAC` 0.15 + `MIN_BYTES` 64 MB + both ends), so wait time scales with FILE SIZE when what actually matters is RUNWAY IN SECONDS against the rate the bytes arrive. Measured 2026-09-14, Terminator 3 4K HDR: 21.89 GB at 29 Mbps, arriving at **82 Mbit/s** — 2.8x realtime, so the frontier can never be caught — yet the 15% gate demanded **3.28 GB = 20.4 minutes of runway** and about 5.5 minutes of waiting for a film that was safe to open in well under one. The clean gate, with `r` = download bytes/s, `b` = playback bytes/s, `D` = duration: **`runway_secs >= D * (1 - r/b)`, floored at ~20-30 s.** It degrades correctly in both directions — surplus rate collapses it to the floor, half-rate demands half the film — and it subsumes the slow-4K case this item was originally filed for. `b` comes from `probe_media_quality` (already computed and already on screen in the quality label); `r` from `TorrentProgress`; (c) dedup `prefetch_ends` (currently fires per /ready poll — harmless pile-up of fire-and-forget tasks, but wasteful); (d) de-prioritise DV profile-5 4K for the VLC target (renders poorly). Design + why below:

**Why VLC hangs on a fresh 4K torrent** (research-confirmed): a player must PROBE the container before playback — track headers (front) + the seek index (MKV **Cues** / MP4 **moov**), and MKV Cues sit at the **END** of the file. Torrents download rarest-first, so the tail (Cues) + sometimes the head aren't present yet; VLC's end-probe (a Range request for the index) blocks on the not-yet-downloaded tail → hangs. "Enough data" isn't quantity, it's the RIGHT pieces (head + tail-index + a sequential lead buffer). webtorrent/peerflix/Popcorn/qBittorrent all solve it identically: **sequential download + "download first AND last pieces first" + buffer-a-threshold-then-launch-the-player** (peerflix's `--vlc` auto-opens VLC when ready).
**Fix (reuse spela's existing `/progress` warmup + SPA loading screen, already built for Chromecast):**
1. VLC path on a PARTIAL torrent: sequential download + **PREFETCH first + last pieces** — seek the librqbit FileStream to `end-1MB` and read (forces librqbit to fetch the Cues pieces), same for the head → VLC's probe succeeds.
2. **Buffer-gate**: don't fire the `vlc://` launch until head+tail present + a lead buffer sized by download-rate-vs-bitrate.
3. **AUTO-launch**: SPA shows the `/progress` screen on a VLC play, polls, and auto-fires `vlc://` when spela signals "ready" — no manual quit + re-click.
4. **4K on a slow swarm** (rate < bitrate → can't stream live regardless): gate to FULL (or rate-safe) download, THEN auto-launch the static serve (smooth + seekable). User never re-clicks; it "starts when ready."
Check librqbit for a sequential / first-last-piece priority API; else the FileStream head+tail read is the primer. Ranker note: DV profile-5 4K renders poorly in VLC anyway — consider de-prioritising DV for the VLC target.

### VLC audio/seek reliability
- **Away-from-home playback — `spela-vlc` shipped on MERIAN 2026-08-13; fleet-ify it.** One-command watch on a VPN'd Mac: `~/.local/bin/spela-vlc "<title>" [--movie]` searches spela + opens the top source in VLC (`…/open.m3u`, subs+resume) → HDMI/Cast to a foreign TV (spela can't cast to a non-home-LAN TV). Currently MERIAN-only + UNTRACKED → weave into dotfiles/`~/.local/bin` so every Mac gets it + it survives reprovision. Optional upgrade: install the `vlc://` handler on MERIAN (fussy on Big Sur over SSH) so the web remote's "Open in VLC" button works one-tap there. Full: CLAUDE.md Hard-Won "Watching AWAY from home".
- **Metered-connection detection — DONE 2026-09-06.** Off-LAN (`10.x`, i.e. WireGuard, which is the only way in from mobile data or a hotspot) defaults the search to Saver when no preference is asked. `X-Forwarded-For` is read but trusted ONLY from a local peer, since the remote arrives through kamal-proxy. The known imprecision stands and is accepted: off-LAN is a superset of metered, so a laptop on a café's wifi is capped needlessly — the quality control covers it. `navigator.connection` was rejected: `effectiveType` measures speed, not cost.

- **Bitrate in the quality label — DONE 2026-09-06.** Shipped as an ffprobe of the file actually being served, not from VLC's `stats.demuxbitrate` (instantaneous, moves with buffering, undocumented unit) and not from the release name (a claim; two proofs in this project that it lies). File bitrate = size ÷ duration, total rather than video-only. Verified against an independent ffprobe on the same file: 6,173,439 bps reported vs 6,173,440 measured. Probed once per path in the background, cached, keyed by path so a Bypass substitution changes the label with the bytes.

- **Auto-subtitles on VLC — the live-verify RAN 2026-09-05 and FAILED; fixed, verified end to end.** VLC ignores `input-slave` from a playlist file, so the whole OpenSubtitles fallback had reached VLC exactly never (measured: same URL fetched as a CLI flag, zero requests from an m3u). The `vlc://` handler now passes `--input-slave`; subtitles are cached per episode key and warmed from `/vlc/{id}/ready` (cold 39.7 s → warm 0.007 s), and a missing one is negative-cached because VLC retries a failing slave ~4 times. The ranker also gained a language tier — a Polish AI-dub and a French MULTI had been outranking the plain English release on seeds alone. Full: CLAUDE.md Hard-Won "A subtitle reaches VLC only as a COMMAND-LINE flag". **Still open:** library→VLC subs (`/library/vlc.m3u` has no sub endpoint, so `--input-slave` is only attached for `open.m3u` plays).
- **Library→VLC seek — FIXED 2026-08-05 (serve-library sliding TTL).** VLC seeks died ~2 min into a library watch (handle expired at the 120 s TTL → 410 GONE → "input can't be opened"). Now the handle TTL is SLIDING (refreshed on every stream access) + a 6 h base. Full: CLAUDE.md Hard-Won "serve-library handle TTL is SLIDING". **Follow-up (open): library→VLC "never a dub"** — `/library/vlc.m3u` serves the raw file, so VLC picks the container default audio track; pass the TMDB `original_language` as an `?al=`/`#EXTVLCOPT:audio-language` hint (the vlc:// handler already forwards `?al=`). Needs the library entry to carry `original_language`.
- **Audio drops — device-pin CURE SHIPPED 2026-08-04 ✅ (one live-verify left).** The macOS auhal output module was resetting mid-stream when VLC followed this Mac's system-default-device churn (8 devices; VideoLAN #8556). Cure: the `vlc://` handler now resolves the Argon DA2 DAC's current CoreAudio id FRESH each launch (by its stable Burr-Brown UID via `SwitchAudioSource`) and passes `--auhal-audio-device=<id>` — self-correcting against reboot/replug id drift; DAC absent → no flag → graceful fallback. First attempt (`--audio-device=<UID>`) was an INVALID VLC option and broke launch (Built≠Verified); the correct flag `--auhal-audio-device=<numeric id>` is verified-accepted. Fredrik confirms drops stopped. `switchaudio-osx` tracked in `packages/Fredriks-Mac-Mini.brewfile`. **Remaining:** confirm the pin holds across a real reboot/replug that shifts the id (resolver handles it by construction, unproven on an actual drift event). Full: CLAUDE.md Hard-Won "VLC audio drops + seek-kills". Monitor: `~/.local/bin/spela-audio-drop-monitor`.
- **Seek-kill recovery can land on a partial source.** 2026-08-03: a backward seek dropped a HotD S03E06 stream (complete 588MB MeGusta), and the re-open played result #4 — a *different*, still-downloading 3.8GB release — via FileStream, so it couldn't resume. Consider: for the VLC path, prefer a COMPLETE non-sparse on-disk copy of the episode over a sparse/partial one regardless of which result id was tapped.
- **Amp signal-sense keepalive during playback (parked).** Earlier amp-mute hypothesis was DISPROVEN (VLC decoded audio fine through the silence); the during-playback keepalive idea is moot for this bug. Only revisit if a genuine idle-APD case appears.

### VLC remote control (v3.22 — shipped, one live-verify left)
- **Confirm the full couch-loop live.** Web remote → VLC control is deployed (relay: `POST /vlc/control` → watcher drains `/vlc/pending` @1s → VLC HTTP cmd). Endpoints round-trip-verified + the watcher reloaded, but the end-to-end (tap play/pause/seek on the phone → VLC-on-the-Mac responds within ~1-2s; scrubber tracks VLC's playhead) wants ONE real watch to confirm. If a command lags >2s, the watcher's 1s drain tick or the `spela-vlc-bridge` agent (check `~/.local/share/spela-vlc-bridge.log`) is the place to look.
- **VLC now-view liveness/ending.** It does NOT auto-clear on `!vlc_active` (that flag is only fresh ~30s after VLC's last byte fetch — a buffered-ahead quiet VLC would look "ended"). The user stops via the Stop button. If a cleaner auto-end is wanted, gate on "position not advancing AND /status quiet > ~3min".

### Graceful restart (shipped + deployed 2026-08-04, one live-verify left)
- **Confirm Chromecast boot-resume across a restart.** Socket-activation is proven live (both listeners inherited, no address-in-use) and VLC reconnect-across-restart is verified (VLC re-requested a mid-file byte-range and resumed after a real restart). The untested path is `maybe_resume_stream_on_boot`: after `systemctl restart spela` with a live cast, the Chromecast should re-establish (transcode + cast from the persisted `CurrentStream`, recency-guarded ≤180min) at the right position. Needs Fredrik present (a restart interrupts the cast). Kill-switch `SPELA_NO_RESUME=1` if it misbehaves.

### v3.19 open loops (2026-07-29 — followed-shows tracker + VLC playback tracking)
- ~~VLC-torrent FD leak~~ **FIXED 2026-08-04** (`reap_previous_vlc_torrents`): each new Open-in-VLC play stops the previously-started VLC torrent (you watch one at a time), keeping the data on disk for Local-Bypass. Bounds VLC torrents to the current stream, so per-play torrents can't accumulate into `EMFILE` again. `LimitNOFILE=524288` (drop-in, hemma overlay) stays as the ceiling. Residual (harmless): the LAST torrent lingers after VLC quits until the next play or a restart — 1 torrent's FDs is negligible.
- **Follow-list curation — now UI-driven (v3.20).** Follow/Unfollow buttons on the search card + an unfollow ✕ on New-Episodes rows; no manual JSON editing. `following.json` (Darwin, user-local — never the public repo) is a membership set; progress lives in the ledger, so unfollow/re-follow is lossless. Residual: none beyond adding shows as viewing changes.
- **VLC watcher end-to-end validation.** Confirm on a real VLC watch that `/position` syncs the resume HWM live + completion auto-marks (heartbeat in `~/.local/share/spela-vlc-bridge.log`).
- **VLC web-interface persistence.** Verify `vlcrc extraintf=http` + `http-password` survive normal VLC quits (VLC rewrites `vlcrc` on quit — it may need re-setting or a guard).
- **spela-vlc-bridge plist not nit-tracked.** `~/Library/LaunchAgents/*.plist` is gitignored; the watcher *script* IS nit-tracked but the plist isn't → add a deploy trigger so a fresh Mac provision installs the agent (fresh-provision gap).
- **Opus 5 / CC auto-update — self-heals, NO action.** The z.ai `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC` var (which had frozen the native updater at 2.1.172) was stripped; the updater resumes on fresh `claude` launches, so ≥2.1.219 + Opus 5 lands on its own. Just confirm eventually.

### v3.14 + peer-supply — SHIPPED (2026-07-05 overnight barrage) + OPEN LOOPS
A 20-title barrage (blockbuster → Vampyr 1932) on an isolated ephemeral-DHT test instance (7891) drove these to production:
- **pkill cross-instance-kill fix** — `do_cleanup` scoped to `<media_dir>/transcoded_hls` (was SIGKILLing production's live ffmpeg from a *test* instance's cleanup; the ORIGINAL looping-Silo cause).
- **alass densest-track + fast-extract** — wrong-release SRT 6-min-offset fix; picks the densest embedded text track (non-forced-first, stop at first ≥20-cue) = ~1 extraction (was extracting all 6 = 16s "Preparing").
- **torrent pre-buffer 20→10 segments** — ~45% faster start (barrage median 44s→24s, 0 buffering; validated by a 3-min sustained play racing 4→124 segments ≈ 4× realtime).
- **20-tracker injection** (session-wide `SessionOptions.trackers`) — peer-supply for thin swarms (Vampyr 62→23s, Hundstage 0-seed >75→22s).
Data + research: `docs/barrage_findings_2026_07_05.md`, `docs/slow_torrent_strategies_research_2026_07_05.md`, `docs/librqbit_streaming_faststart_research_2026_07_05.md`.

**OPEN LOOPS (Fredrik decisions + deferred work):**
- **[TRACKER-RESILIENCE THREAD CLOSED 2026-08-05 — Fredrik accepts a few outages/year; no fallback built]** Explored mirror-fallback + second-scraper resilience after a Torrentio 522 outage. Findings: **no config-less public second-scraper is reachable from a server** — Comet (elfhosted) is Cloudflare-blocked (403 even with a browser UA); KnightCrawler 302s to /configure; MediaFusion returns 0 config-less; Jackettio returns a *"kindly configure this addon"* nag (0 real streams); and Torrentio has effectively one host, so "mirrors" aren't a thing. The genuinely-robust options were TorBox debrid ($3/mo, also the 4K cure — below) or self-hosting KnightCrawler on Darwin (Postgres + indexer stack + maintenance). **Fredrik declined TorBox and the self-host** — the shipped 522-retry (8 s timeout, 2 attempts, clear "temporarily unavailable, try again" message; `search.rs::torrentio_streams`) covers the common blip case, and a full Torrentio outage (~1-3×/year) is tolerable. Do NOT re-propose unless Fredrik reopens it (revisit only at launch). Detail on the shipped 522-resilience: CLAUDE.md Hard-Won "Torrentio 522/non-JSON".
- **[PARKED — Fredrik decision, revisit at launch] TorBox debrid fallback — now also the 4K-reliability cure** — the *other* cure for obscure/slow swarms (~$3/mo, ~200 lines `reqwest`). Deliberately deferred 2026-07-06 (Fredrik: no $3/mo yet; will consider "in future or when I launch/market spela more widely"). **Sharpened 2026-07-13**: this is *the* answer for 4K. HotD S03E04's 4K sources would not download at all — the DV releases connected 1 peer / 0 B/s, the "344-seed" non-DV connected 3 real peers / 0 B/s (Torrentio seed counts are inflated tracker-scrape claims, not deliverable capacity). The 4K browser pass-through is built + ready, so debrid is the *only* missing piece for reliable 4K playback (it holds the file → "seeds" stops mattering). Would slot in as an extra racer alongside the local sources (try local → fall to TorBox if no byte-0 in N s → cached = instant HTTPS). Config-gated (absent token = today's behavior, zero regression). Full API flow + fallback-ladder: `docs/slow_torrent_strategies_research_2026_07_05.md` § 6. Secret in a tier; SSRF-validate the `*.torbox.app` CDN host at the boundary.
- **[DONE + VERIFIED end-to-end 2026-07-13] Browser 4K/HEVC pass-through** — a HEVC source to a browser target is stream-copied into fMP4/CMAF (hvc1, audio→AAC), full 4K, no re-encode/downscale. **VERIFIED**: Chrome/hls.js decoded a real 2160p HEVC at 3840×1920, playing. Reaching "works" needed a fix-chain (all in CLAUDE.md Hard-Won): ffprobe field-order parse (`video_codec` came out empty → gate silently failed; `codec_name` precedes `codec_type`), **HEVC→browser is ALWAYS fmp4, not just 4K** (regular transcode copies HEVC into mpegts which browsers can't decode → 0 segments → fail-fast), master CODECS `.B01`-tail strip (Chrome MSE rejects `hvc1.2.4.L150.B01`), hls.js buffer bounded for huge 4K segments (else SourceBuffer quota-overflow → spin + reseek jumps), bare-live playlist so ffmpeg's ENDLIST→VOD kills the live-sync frame-jump. Runway 12s/10s→30s (2160p), search cap 8→40. Commits `abba80c`…`4b397c0`. **Follow-ups**: (a) auto-select best 4K on a 4K display (today it's a manual "More sources" pick); (b) prefer non-DV over DV for browser 4K (profile-5 tint; 8.1 fine) — the VLC target sidesteps this (VLC decodes DV cleanly).
- **[SHIPPED 2026-07-13] Open-in-VLC** — "VLC · this Mac" target → opens the source in VLC (native HEVC/DV, full seek). `/vlc/{id}/stream` (Range; all 3 states: complete-file / partial / fresh-torrent FileStream) + `/vlc/{id}/open.m3u` (carries `#EXTVLCOPT:audio-language`+`?al=` = original-language audio rule). One-click `vlc://` handler tracked at `~/dotfiles/scripts/install-vlc-url-handler.sh`; SPA fires vlc:// + .m3u fallback. **Follow-up (deferred, needs a restart):** picking VLC from the *now*-view doesn't open the current title (only a fresh source tap does) — store `S.curRid` + route the now-view VLC target through `openInVlc`.
- **[SHIPPED slices 1-2-3arsenal, 2026-07-13] Watch tracker (spela replaces TV Time — free/no-ads/no-sub).** Slice 1 `/recent` "Recently watched" wrapping row (last-14d distinct plays; tap→jump-to-show; NOT resume). Slice 2 watched-ledger (`AppState.watched` auto-marked at completion, `/watched`). Slice 3 ARSENAL: `/watchlist` + "To Watch" nav view (RT-scored rows, <88 dimmed, watched hidden, tap→search) serving `~/.config/spela/watchlist.json` (seeded from RT import, 54 movies + 17 unwatched series). **⚠ arsenal data lives on the SPELA HOST (Darwin `~/.config/spela/`), NOT the Mac — editing on the Mac won't reach spela; scp to Darwin.** **NEXT (trued 2026-08-04):** slice 2b Up-Next next-unwatched episode is SHIPPED (`next_unwatched_skipping` → `/home` surfaces `next_unwatched` + air date); slice 3 HARNESS has RUN (`recommendations.json` live on Darwin — Claude-curated picks with `why` rationales + `taste_profile.md`). Genuinely remaining: the airdate→**push-notification** when a followed show's new episode drops; slice 5 auto-track house Chromecasts (cast-status polling; bg-loop needs Fredrik's OK per bg-task-guard). Roadmap: `docs/watch_tracker_roadmap.md`. Personal data USER-LOCAL in `~/.config/spela/`. **Open capture:** TV Time GDPR export is empty of history (verified); Fredrik screenshotted the completed-series profile pages but iCloud sync is full — pending; naming loved shows into `watchlist.json`/taste profile meanwhile. **Structural fix for the whole gap: a one-time watched-history import into the ledger** — the recommender still guesses what he's already seen because the ledger is recent-spela-only, not lifetime. **Sharpened 2026-08-26** after the gap bit FIVE times in one session (The Lowdown, The Pitt, Oxygen, I Am Mother and Upgrade were all recommended and all already loved): the recurring variable is the LEDGER, not the picks, so a recommender should SAY it is guessing rather than present picks as discoveries (now written into `taste_profile.md`). **Newly-identified path — Fredrik had assumed none existed**: Netflix publishes full per-profile viewing activity at `netflix.com/viewingactivity` as a downloadable CSV, and Amazon has an equivalent data request; between them that is likely a large slice of a lifetime of viewing (Apple TV has no export). **Use the PER-PROFILE page, not the account-level GDPR export** — he has always used his own profile on the family account, and the account export pulls everyone. **Blocked on**: the Bitwarden vault is logged OUT (`status: unauthenticated`, `lastSync: null`), so the Netflix credentials can't be looked up — Fredrik runs `bw login` then `bwu` (master password, human-only), then Claude can search the vault and build the importer. Trakt/Letterboxd remain the fallback if the Netflix route disappoints.
- **[DONE 2026-07-08] Persistent play-error/retry surface + mobile layout** — a failed play now shows "Couldn't start — <reason>" + Tap-to-retry ON the now-view (was toast-only + bounce-to-Search). Mobile: icon-only nav + Chromecast pill, full-width search result, sideways-overflow killed (`@media(max-width:560px)`). Commit `9341379` + the layout commit. UX-doc §6/§9 + §7 cheat-sheet now fully shipped Claude-side; only the custom-receiver track (subtitle-as-track, intro-warmup) remains, gated on Fredrik's $5 Cast registration.
- **[DONE 2026-07-06] WAN inbound listener re-opened** — TCP 6881 accept re-added to the WAN iface (`enx00e04c680072`) in iptables INPUT, persisted in `dotfiles/system/darwin/etc/iptables/rules.v4`. The 2026-07-05 precautionary close was reverted: the e1000e hang's root cause was the NIC TSO/GSO offload bug (fixed by offloads-off + `eno1-nooffload.service`), NOT the listener — the hang was on `eno1` (LAN) while the listener is on the WAN iface. CrowdSec per-IP bans still cover 6881. (µTP inbound over UDP 6881 not opened — only the TCP peer listener; add UDP if µTP inbound is wanted later.)
- **[Fredrik decision] Public-repo personal data** — `semikolon/spela` is PUBLIC and CLAUDE.md + docs carry device names / `192.168.4.x` / `/home/fredrik` / `darwin.home` (pre-existing ~14 lines, compounded this session). Risk is LOW (private RFC-1918 IPs, no secrets, name already public via the org) but violates the repo's own NO-PERSONAL-DATA rule. Decide: make private / sanitize-to-placeholders / accept. (The test-instance launcher was deliberately kept OUT of the repo — it lives at `darwin:/home/fredrik/spela-test-instance.sh` — pending this.)
- **[DONE 2026-07-06 — local variant, LIVE on prod] Source racing (no debrid)** — weak-swarm-gated pre-selection stage in `do_play`'s torrent branch: when the top pick has `seeds < race_seed_threshold` (default 100), the top `race_max_sources` (default 2) releases are added to the shared librqbit session concurrently, the first to deliver real bytes wins, losers are stopped+deleted, winner continues via `AlreadyManaged`. Config-gated (`race_sources_enabled`, default on). **Validated on the test instance** (The Cremator 4-seed → result 2 delivered first, won in ~1.3s, downloaded at 843 KB/s). Two commits: `2857992` (feature) + `047bd75` (concurrent-start fix — `add_torrent` blocks on magnet metadata, so a dead candidate-1 hung the sequential version). Debrid leg parked (above). GPU use unchanged (race is download-only; only the winner transcodes).
- **[good hygiene] deliberus-postgres binds `0.0.0.0:5432`** — WAN-blocked (INPUT drop + DOCKER-USER) so LAN-only, but should bind the LAN IP like the other DBs.

### NEXT — intro-warmup: instant TV wake + parallel handshake (Fredrik's idea, 2026-07-04)
The ~8s Chromecast LOAD handshake (connect + launch receiver app + buffer) runs SERIALLY *after* race-ahead. Fredrik: cast the (currently-disabled) intro clip as a warm-up placeholder the instant the transcode starts → TV lights up in ~2-3s (huge perceived-perf win) + the receiver-app-launch overlaps the transcode buildup → then LOAD the real HLS on the warm receiver. Build defensively (best-effort; any failure falls through to the normal cast, no regression). Timing caveat: intro is 5s, real-ready is ~13s → a gap where the intro ends before real content; consider looping the intro or a longer placeholder. NEEDS live-Chromecast testing (the swap-glitch) — can't fully verify without a device.

### NEXT (the REAL fix for BOTH speed + subtitles) — custom Cast receiver ($5 registration)
Fredrik's insight (correct): Cast receivers render subtitles as a **layered text track**, NOT burned in. With a custom receiver we can (a) cast the real HLS as soon as 1 segment exists (~2-3s) and let the receiver buffer gracefully — no 12s pre-buffer wait; (b) attach the subtitle track SEPARATELY, aligned in the BACKGROUND, added after playback starts — eliminating the subtitle-blocks-start cost (the 19s cold-cache alass) entirely. **Fredrik plans to do the $5 Cast SDK Developer Console registration ~2026-07-05 — unblocking imminent.** Once registered, this becomes the top spela priority: it subsumes BOTH the intro-warmup (cast-early, receiver buffers gracefully → no ~8s handshake wait, no cast-connection-reuse refactor needed) AND the alass-on-critical-path problem (subtitles as a background-aligned track added after playback starts). Design + build it WITH Fredrik (needs live-receiver iteration).

### NEXT — cold-cache subtitle prefetch during search
First-play subtitle cost is the cold alass audio-VAD (~15s, now capped at 5s → raw sub fallback). Prefetch + align subtitles in the BACKGROUND when the user views search results (before they tap play) → by play-time the aligned SRT is cached → fast first-play WITH aligned subs. Decouples subtitle latency from the play entirely (for the DMR-burn-in path; the custom receiver makes it moot).

### NEXT (architectural, deferred): decouple `/play` from the HTTP request lifecycle
The proxy-timeout bump (300s) is the robust interim, but the *proper* fix is to make `/play` return fast (right after starting the transcode) and run the cast-gate + cast + monitor in a DETACHED tokio task that the HTTP request's cancellation can't kill. Then no proxy/browser timeout can ever orphan a cast, and `/play` is instant. The warmup UI already polls `/progress` + `/status`, so the frontend is ready — needs: (a) `/play` spawns the cast pipeline detached + returns `{status:"starting"}`; (b) background-task errors surface via a `/status` `last_error` field; (c) SPA keeps the warmup screen until `/status` shows streaming (don't `endWarming` on the fast `/play` return). Also consider lowering the race-ahead `MIN_LEAD_SECS` (120s → ~40s) for COMPLETE local files — a 3.5× transcode of a complete file never gets frontier-caught, so 120s of pre-buffer just adds ~30s to every start; a smaller lead would make Chromecast plays start in ~15-20s instead of ~50s.

### samklang — torrent-streamed music extension (SPEC SHIPPED 2026-06-02, NOT YET IMPLEMENTED) 🎵
Three-file Kiro spec at `.claude/specs/samklang/{requirements,design,tasks}.md`. Music streaming for a small invited circle, built as `MediaType::Music` extension to spela (one binary; `/music/*` routes; new `/listen` tab in `static/remote.html`). Torrent-only streaming (BT primary v1, Soulseek + Internet Archive in phase-4 try-harder path), progressive in-time quality upgrade via Web Audio + MSE source-swap (mp3 → FLAC seamlessly), bearer-token multi-user, Postgres on Darwin :5433 for state. 5 phases ~5-6 weeks at AI-augmented velocity; **phase 1 weekend-shippable** (search + play + web /listen tab, single user, Jackett-on-Darwin-Docker as the indexer hub). Cross-cutting non-regression invariant: existing video paths (`MediaType::{Movie, TvShow}`) bit-identical throughout. Companion: dotfiles (B3) Shannon BT-audio side-quest must resolve before T-3.11 Marshall-via-Shannon target can land — Chromecast cast (T-3.7) ships first.

---

### Phase-7 spela-thin-client (Shannon bedroom, kiosk Option A) — architectural keystone LIVE · HW-decode regressed since 2026-05-21 · Watch UI is the next UX rank-1
Architecture: Shannon plays Darwin's no-cast HLS via a local renderer; Darwin's NVENC does all transcoding (research § 6-compliant H.264 8-bit). Two entry points to the same plumbing:
1. **Bevy Watch tile** → daemon `POST /watch` → spawns `spela-local` → /search + /play target=vlc on Darwin → fetches HLS + decodes locally
2. **Spela web remote "Shannon TV" target** (2026-05-23, `ffa22bf`) → spela `POST /play target=shannon` → Darwin spela POSTs to Shannon's daemon `/watch` → same downstream

**Authoritative deploy spec** = kiosk research § 6 (`~/dotfiles/docs/shannon_kiosk_gpu_hwaccel_research_2026_05_18.md`). Do NOT duplicate the architecture here.

**Live state**:
- ✅ Bevy keystone (kiosk `7233c34`+`ff662ae`) + daemon `/watch` route + `spela-local` shell
- ✅ Spela `target=shannon` + web remote "Shannon TV" button (`ffa22bf`)
- ✅ Daemon LAN-bound (`0.0.0.0:8080` since 2026-05-23) so Darwin spela can reach it
- ✅ Two-path renderer in spela-local: Path A (HW: fdsrc+tsdemux+h264parse+v4l2slh264dec+kmssink) with 8s preroll budget → Path B (SW: playbin3+kmssink+autoaudiosink) auto-fallback
- ✅ MODE=blank dotfiles drift fixed (`c35e194e`)
- ✅ **HW decode VERIFIED 2026-05-26 — root cause was SILENT REGRESSION, not driver issue**: 2026-05-26 diagnose-first session confirmed cage+waylandsink runs at ~33% total CPU sustained (gst-launch 24% + cage 9%) at 1080p H.264 + AAC, rkvdec engaged at /dev/video2 the whole time. The "99% CPU" was the SW fallback firing because cage exited immediately at startup with `XDG_RUNTIME_DIR is not set`. Fix shipped dotfiles `b09297d4`: scoped /run/cage-spela-local + WLR_LIBINPUT_NO_DEVICES=1. Path A v3 + Path B+ retained as fallbacks.

**Open items, RE-PRIORITIZED 2026-05-23**:

| Rank | Item | Effort | Note |
|------|------|--------|------|
| 1 | **Watch UI mirrors spela web on Shannon** — search + library grid + now-playing + scrubber natively in Bevy | medium-high (Bevy + spela polling) | Fredrik's explicit direction. Watch button currently hardcodes ONE smoke title; this is the biggest unrealized UX value. |
| 2 | **Rust gstreamer-rs renderer** (audio branch + reliable Path A→B fallback + better diagnostics) | high (~3-6h) | Was promoted to rank-2 thinking it would fix the rkvdec "regression"; that regression was a log-filter-omission misdiagnosis (see closed-loops below), rkvdec is HEALTHY. Renderer still valuable: (a) audio branch needs pad-added signal handling that gst-launch syntax can't express — the `tsdemux d. ! aacparse` syntax fails at construction-time because tsdemux pads materialize only on data arrival; (b) deterministic Path A→B fallback instead of spela-local's shell heuristic; (c) cleaner per-V4L2-ioctl error visibility. Design doc + Cross.toml gstreamer-libs delta: `~/dotfiles/docs/shannon_gstreamer_rs_renderer_design_2026_05_24.md`. |
| -- | **Cast init silent-failure — NEW SCOPE (Jun 4, 2026 movie-night reproduction): bug fires on LOCAL-FILE path too, not just torrent-stream + sparse**, expanding original 2026-05-24 framing | **diagnostic gates [1-4] still never fired on the failing attempt** — exit point is BEFORE the cast block | Jun 4 anchor: Riders of Justice play, file fully downloaded on disk (Local Bypass MATCHED), ffmpeg ran clean for 140s producing 264 HLS segments at 11× realtime → `race_ahead_safe(1584s buffered, 140s wall)` math said go but `wait_for_complete_hls_before_cast` never returned. Status idle, no cast-init [1/4] log line. **Workaround that broke the hang**: `catt -d Vardagsrum stop` to reset the receiver app — implies stale receiver-side cast session was somehow blocking the new init. After catt reset + spela stop + retry: cast flowed normally through all 4 gates. **Next investigation should check** (a) whether spela's `cast.cast_url` holds state across attempts that becomes inconsistent with the actual receiver, (b) whether `wait_for_complete_hls_before_cast` returns Ok but the next-line cast spawn hangs, (c) why race-ahead-safe path doesn't log the success message even when math passes. Diagnostic gates [1-4] still in place from `8345a77`. Original 2026-05-24 torrent-stream + sparse framing: triggered The Boys S05E08 cast TWICE (21:05:14 + 21:06:49) — both attempts had ffmpeg HLS pipeline started + Local Bypass rejecting (sparse 2.8GB < logical 4.7GB) + falling through to torrent-stream source path → same silent-init symptom. Docker container 172.18.0.4 (HA) spamming `/torrent/*` getting SSRF-rejected was present in BOTH incidents — possibly correlated, possibly red herring. |
| ✅ | **`/play 1` stale-last-search-fallback — SHIPPED + DEPLOYED + LIVE 2026-05-25 (commit `a878b19`)** | done | Originally rooted-caused at `server.rs::handle_search` L719-740: the function only called `AppState::save_last_search` on the `Ok(result)` arm; on `Err(e)` the previous successful search's `last_search.json` remained untouched and a subsequent `/play 1` happily resolved against stale data, replaying yesterday's stream (Ex Machina dispatch → The Boys S05E08 playback, smart-resume auto-jumped to 661s). Fix: Err arm now writes an empty `SearchResult { query, error: Some(e.to_string()), results: vec![] }` so the next `/play N` bombs cleanly. Deployed on Darwin 2026-05-25 14:34 — binary mtime after commit time, spela.service restarted same window. Verification of the running binary done by chain-of-evidence (commit → build → service-start timestamps); functional Err-arm verification requires either a TMDB/Torrentio outage or test-harness mocking, deferred. |
| ✅ | **Watch A-press playback chain — BACKEND VALIDATED end-to-end 2026-05-28 via uinput** | done (live-TV visual still nice-to-have) | Original report (Fredrik 2026-05-25 evening): *"on Watch menu: spela library list shows up on TV, can scroll through it and I did at one point try pressing A to play something but hit some bug back then."* Root cause was the stale-last-search-fallback (already shipped a878b19). 2026-05-28 validation via `kiosk-inject-gamepad` (dotfiles `039f7342`, uinput Xbox 360 emulation): BTN_SOUTH on Watch tile → opens WatchSubmenu showing live library (Aladdin, American Psyco, Angel Heart, ...). BTN_SOUTH on Aladdin → `try_dispatch_watch_post` → daemon `/watch` → spela-local session start (`magnet=<none,title-search>`) → Darwin `/search` succeeded → Darwin `/play` accepted → HLS transcode cold-starting. Aborted at 5 sec into cold-start (didn't let it actually render on TV). All handler paths through the full chain — Bevy South-arm + daemon dispatch + spela-local spawn + Darwin transcode — proven by log evidence. The only piece NOT directly tested: visual + audio output on the TV during HW-decoded playback (waylandsink HW scanout is invisible to shannon-screenshot per its documented limitation). Cheap to verify next time Fredrik's at TV + controller. |
| -- | **spela-local Path A "alive after preroll" false-positive — SHIPPED 2026-05-25 (dotfiles `f8e88e03`)** | done | 2026-05-24 evening: live HLS Path A pipeline emitted `ERROR: not-negotiated (-4)` from `fdsrc0`, THEN spela-local's alive-check logged `"Path A is alive after preroll; continuing on HW"` 4 seconds later — wrong verdict. Bare `kill -0 $PATH_A_PID` passed during gst-launch's EOS shutdown window. Fix shipped 2026-05-25: capture gst-launch stderr to `$PATH_A_STDERR` tempfile, gate alive-verdict on (process-alive AND no `ERROR:` line in stderr). Logs the matched ERROR lines before falling back so future debugging sees the actual cause. Deployed to Shannon via `scp shannon:/usr/local/bin/spela-local` + atomic install + syntax-check (`bash -n`) — old version backed up to `/usr/local/bin/spela-local.bak-2026-05-25`. |
| ✅ | **MAGNET passthrough on target=shannon — SHIPPED 2026-05-26 end-to-end** | done | Closes the silent result-divergence bug. 3-layer chain: Darwin spela `target=shannon` dispatch forwards `magnet`+`file_index` from already-resolved `last_search` (commit `c6e3de6`); shannon-kiosk-actions `/watch` accepts both fields with length/control-char validation (commit `1cde18b`); spela-local takes `--magnet` + `--file-index` and SKIPS its own `/search` round-trip when magnet supplied (dotfiles `58c677c6`). End-to-end smoke verified 2026-05-26 09:36: `/play {result_id:1,target:"shannon"}` → Darwin `direct_play=true` log → Shannon `magnet=<supplied,424b>` + `direct-play (magnet supplied... skipping /search)` log. Title-search fallback preserved for My-Library taps + manual operator invocations + voice assistant. Chose magnet primitive over `result_id` because it eliminates Shannon's last_search dependency entirely (Shannon would have needed its own /search to populate last_search). |
| 5 | **Live verify Music + Sensors + Buses on TV** | trivial (just observe) | Code shipped (`ff662ae`); presence-on-TV unconfirmed since deploy. Sensors-page sparkline rendering bug found 2026-05-23 (Block Elements U+2581-2588 rendered as TOFU because SharpSans lacks those glyphs + Bevy 0.18 doesn't font-fallback) → quick fix shipped 2026-05-24 (ASCII ramp `_.,-=+*#`). Proper fix (TextSpan per-codepoint fallback OR bundle Unicode-complete fallback font OR render sparklines as Bevy mesh bars) deferred — tracked separately. |
| 6 | **Buses-A "crash" repro + Bevy freeze diagnostics** | medium | Fredrik reported kiosk freeze after A on Buses tile (which is a `_ => {}` no-op). Either a kiosk-level state issue or controller disconnect mid-press. Need `journalctl -u shannon-display -n 50` + `/var/log/spela-local.log` captured at next freeze. |
| 7 | **Audio in spela-local** | medium | The explicit `tsdemux name=d ... d. ! aacparse ! avdec_aac` audio branch hits `not-negotiated` at preroll. Likely needs the gstreamer-rs renderer (#4) to do pad-added signal handling properly. |
| 8 | **HW-decode 33%→15% — PROBE RAN 2026-05-28 evening; decision tree answer = (c) HTTP/HLS** | medium (queue/source tuning) / high (Path B) | Current shipped state = 33% CPU sustained (cage+waylandsink+v4l2slh264dec). Werner reports 15% on RK3399 Bookworm+Kwiboo (`~/dotfiles/docs/reflections/2026-05-25-rk3399-hw-decode-canonical-survey.md` § H.6). **EMPIRICAL DIAGNOSTIC RAN 2026-05-28 17:50-17:52 CEST** during a live Inception playback (kiosk-injected BTN_SOUTH → spela-local cage+waylandsink HW pipeline). Used `pidstat -t -p $GST_PID -u 1 N` per-thread breakdown (perf not installed on Shannon kernel 6.18-current-rockchip64; per-thread thread NAMES are sufficient for the decision tree without kernel-symbol depth). **Sustained 30s sample**: cage=5.86%, gst-launch=26.09%, total ≈ **32%** (matches the documented 33% baseline closely). **Per-thread top contributors** (15s average): `souphttpsrc` 2.20% + `souphttpsrc2:sr` 0.60% + earlier ticks showed individual souphttpsrc samples spiking to 5-7% during segment fetches; `queue1:src` 2.47% + `queue0:src` 1.93% + `queue2:src` 0.40% (combined ~4.8% buffer feed); `audiosink-ringbuffer` 1.80%; `GstWlDisplay` (waylandsink) 0.40%; `audioconvert + audioresample` BELOW the 0.1% noise floor (not even visible). **DECISION TREE ANSWER**: (a) videoconvert/detile = **NO** — pixman/convert_NV12 symbols nowhere in top threads; decoder + waylandsink negotiate cleanly via DMABuf; (b) audio chain = **NO** — audioresample/audioconvert below noise floor, only audiosink-ringbuffer visible at 1.8%; (c) **YES — HTTP/HLS fetch + buffer management is the dominant remaining eater**. souphttpsrc (HTTP/HLS fetch) sustains pulses of 2-7% per segment + queue buffer management ~5% combined ≈ 7-12% of total CPU is HLS plumbing, not decoding. **CONFIRMS Architecture A (v4l2convert RGA) would NOT help much** — there's no detile work to absorb. **Concrete next-step recommendations from this evidence**: (1) **tune queue sizes** — current `max-size-buffers=200 max-size-time=0 max-size-bytes=0` may be too small for HLS+HW-decode bursts; larger time-based buffers (`max-size-time=10000000000`, 10 sec) might smooth fetch spikes; (2) **switch to gstreamer-rs souphttpsrc replacement** (or a wrapping process that pre-fetches segments to a local fifo, decoupling fetch latency from gst's main loop); (3) **persistent HTTP/2 connection** to Darwin spela (souphttpsrc opens fresh connection per segment in default; HTTP/2 keep-alive across segments would cut TLS handshake overhead). **Path B (Kwiboo mpv via apt.undo.it Bookworm)** is the proven 15%-CPU path BUT (i) requires Bookworm rollback (current Trixie has the documented "Unsupported hwdec: drm" regression), (ii) probably gets 15% partly because mpv uses a fundamentally different HLS+demux+decode path than GStreamer's element chain — the souphttpsrc/queue overhead we measured here is GStreamer-specific. The path to 15% via the GStreamer stack would need the above queue+source optimizations PLUS likely a different compositor (cage's 5.86% may be near-irreducible without a more minimal scanout path). **Persisted recommendation 2026-05-28 evening** (cross-ref: canonical-survey § H.6, zerocopy-architecture § "Diagnostic action order"). |
| 9 | **Layer-1 cage + `vo=dmabuf-wayland` zero-copy** | high | cage is single-client; needs investigation of subsurface support or process-swap reshape |
| 10 | **Layer-3 evdev → IPC controller shim for playback control** | medium | Pause/seek during playback. Currently mpv/gst runs unattended until EOF or smoke timeout. |
| 11 | **Cross-build patched ffmpeg+mpv from Kwiboo/jernejsk source** | high | Fallback if gstreamer-rs path also fails. apt.undo.it remains offline. |

**Closed loops** (no longer pending):
- ~~Layer-4 keystone~~ ✅ LIVE
- ~~Layer-4a (daemon /watch)~~ ✅
- ~~Layer-5 (dotfiles spela-local)~~ ✅
- ~~spela-local curl timeouts~~ ✅
- ~~scanout handoff (Option β)~~ ✅
- ~~MODE=blank dotfiles drift~~ ✅ `c35e194e`
- ~~GStreamer rank-syntax gotcha~~ ✅ documented + numeric `300` used
- ~~Rust `spela --local-renderer` path~~ ⏸ permanent hold (shell client supersedes; bring back only if signed-binary distribution becomes a need)
- ~~Plymouth boot/shutdown splash + seamless splash→kiosk handoff~~ ✅ shipped 2026-05-24 (`98b6ace5`) — `bootlogo=true` + `console=serial` + initramfs DRM modules + `Conflicts=getty@tty1`/plymouth-quit-wait ordering. Architecture detail in dotfiles `system/shannon/README.md` § "Plymouth boot splash + kiosk handoff architecture". Visual confirmation by Fredrik on next watched reboot pending; technical render path verified via `plymouthd --debug` (scanout buffer set on 1920x1080 head).
- ~~HW-decode rkvdec "regression"~~ ✅ NEVER WAS REGRESSED 2026-05-24 evening — file-level smoke with the SAME pipeline (`filesrc → tsdemux → h264parse → v4l2slh264dec → fakesink`) but the CORRECT debug filter (`GST_DEBUG=v4l2codecs-h264dec:5` instead of `v4l2codecs:6`) shows **300 H.264 frames decoded in <1 sec via rkvdec** ("Output picture 0..12+" lines within 200ms; full 300-frame test file in 0.6s wall = ~500fps HW decode rate). The 2026-05-23 + 2026-05-24-morning "EOSes in <1s with zero V4L2 allocator activity" diagnosis was a **false negative from log-filter omissions** — `v4l2codecs:6` catches CODEC-DEVICE messages (probe-time only) but the per-frame allocator activity is in the `v4l2codecs-h264dec` subcategory. Codex's original 2026-05-23 "regression" framing was the same misdiagnosis class. rkvdec is HEALTHY and was probably never broken. Rank-2 (gstreamer-rs renderer) stays in the table because audio branch + reliable Path A→B fallback + better error visibility are still real wins; just NOT framed as "fix rkvdec" anymore.

**Chromecast retirement is PARTIAL** per research § 6 — spela + direct-URL YouTube only; Netflix/HBO/DRM apps keep the Chromecast (irreducible aarch64-Widevine gap).

### 🔓 OPEN — fleet-track the two Mac-side pieces serve-library depends on

Local library streaming is live (design: `docs/LOCAL_LIBRARY_STREAMING_PLAN.md`).
Two pieces work on the Mac Mini but are not reconstructible from a fresh provision.

- [ ] **The launchd agent plist.** `~/Library/` is gitignored by nit, so
  `com.fredrikbranstrom.spela-library.plist` is deployed and reboot-surviving but not
  tracked. Templatize to `dotfiles/templates/Library/LaunchAgents/…plist.tmpl` with
  `{{ home_dir }}`, wired to the `30-reload-launchagents` trigger, mirroring the
  values already applied live: `KeepAlive { Crashed: true, SuccessfulExit: false }`
  and `ThrottleInterval=60`. MERIAN needs a `[[ -n library_dirs ]]` template gate,
  since an empty `library_dirs` still bails. *Consult-before-shared-infra* — propose
  before implementing.
- [ ] **`~/.local/bin/nightly-sweep`.** A plain executable on the Mac Mini, not
  nit-tracked. Its Phase 4 rotates `spela-library.{out,err}.log` in place (`cat tmp >
  file`, which preserves launchd's open FD inode — the older `mv tmp file` form
  orphans it). Track it, and capture the in-place rotation as the canonical pattern.

### 🔓 OPEN — HLS cache, two sizing decisions left

The cache is complete and live at 12 GiB (`Config.hls_cache_cap_mb`).

- [ ] **Raise the cap to 20480** (20 GB ≈ 60 episodes) once movie-night usage shows
  the extra room is wanted. 12288 was chosen conservatively for Darwin disk pressure;
  live-validate before flipping.
- [ ] **Background transcode-ahead** (deferred): when a watch stops at minute 5,
  finish transcoding the rest in the background to populate the cache. Adds an ffmpeg
  subprocess plus GPU contention, which is why it was deferred rather than built.

### Custom Cast Receiver scaffolds — NOT dead code (leave them)

`handle_seek_restart` (server.rs:5407) + `handle_retry` (server.rs:5536) look like dead stubs by their bodies (handle_retry just returns `{"status":"retry_requested"}` with a TODO). They are **NOT webtorrent-era cruft** — git blame shows BOTH shipped in `a9637a6 "feat: Custom Cast Receiver — intro, seeking, subs, resume, overlay"`. The receiver HTML at `static/cast-receiver.html` actively fetches both endpoints (lines 240 + 398). Deleting either would break the Custom Cast Receiver scaffolding. **They're awaiting wiring as part of the $5-Google-registration Custom Cast Receiver landing** (see "Still Open" → Custom Cast Receiver below). When that work lands, both handlers should be implemented properly: `handle_seek_restart` → call `do_play(seek_to=req.t)` (the same path v3.7.7's `/seek-retranscode` uses); `handle_retry` → call the existing `handle_play` auto-retry path.

### Deferred (audit items NOT actioned, with reasons)
- **H5** Cast-receiver IP allowlist — LAN-IP-config friction without much marginal value over iptables-INPUT-DROP + Host-header + LAN-trust layers already in place. User explicit decision.
- **M10** `do_cleanup` race lock — analyzed: librqbit's `session.delete` is idempotent on missing IDs; the race produces a spurious warning log but no actual leak. Adding sync primitives that aren't strictly needed is bad design.
- **L3-L8** polish items (urlencoded check, magnet logging audit, history date-cap, body-builder unwrap code-smell, CORS preflight, etc.) — low-impact; spot-checked during the H1 work, no concrete leaks.
- **Bearer-token auth** — would require plumbing through spela CLI + Ruby's `run_spela` tool. Future-session item if/when WAN exposure happens (Tailscale, port-forward).
- **Full cast_health_monitor `CastMonitorAction` extraction** — 200+ LOC interacting state across 8 mutable variables; high regression risk for a critical path. Sub-decision extraction captured the most-likely-to-drift constants. Future-session candidate.
- **Test-hygiene `tempfile::TempDir` migration for the existing fixed-label tempdirs** — process-id-namespaced fixed-label dirs are parallel-safe; only loss is RAII cleanup on panic, not a real bug we've hit.

### Corrupt-source-file detection (open) 🔧
- [x] **Auto-detect corrupt source files via ffmpeg.log post-mortem** (SHIPPED Apr 30, 2026): `transcode::inspect_ffmpeg_log_for_corruption` parses `~/.spela/ffmpeg.log` for three signals (EBML parse errors, missing HEVC reference frames, `dup=>100` on summary line). `do_cleanup` records corrupt source paths in `AppState.corrupt_files` HashSet; `find_local_bypass_match` skips entries on subsequent scans. 8 unit tests pin parser semantics + corrupt-skip behavior. First failed cast marks the file; manual `rm` no longer needed.

- [ ] ~~**Auto-detect corrupt source files via ffmpeg.log post-mortem**~~ — Apr 29 incident (Hijack S02E05 MeGusta) silently produced ~5 min of NVENC-duplicated junk past byte 417 MB Matroska corruption. Local Bypass treated the broken file as good on every subsequent play; recast loop wedged 5x at the same `time=2022s` before manual rm. Implementation: after each transcode_hls completion, parse `~/.spela/ffmpeg.log` for (a) `dup=<N>` on final summary line where N>100, (b) any `Could not find ref with POC` lines, (c) any `invalid as first byte of an EBML number` lines. If any match, mark the source file path in `state.json::corrupt_files` and have Local Bypass skip it on subsequent matches. Hard-won lesson context in [CLAUDE.md](CLAUDE.md) § "Corrupt source files defeat auto-recast — wedge isn't on the receiver".

### Still Open

- [ ] **Ruby's pre-confirmation speaks BEFORE the tool call completes** — `conversation_engine.py::_execute_spela` synthesizes "Got it — season X episode Y on your Chromecast" BEFORE running the spela subprocess, so when the subprocess fails (disk cap, dead seeds, etc.) Fredrik hears success but gets no correction. Apr 15 system-prompt update (dotfiles commit `494a82f`) told Gemini to speak an error correction as her first action on the next turn; validate that the fix actually works the next time a play fails. If Gemini still ignores tool errors, escalate to a code-level fix in `_execute_spela`: detect error responses and synthesize + play an error correction TTS BEFORE returning the tool result to Gemini. Don't depend on Gemini's discretion for critical error relay.

- [x] **spela search returns spurious non-matching shows (mitigated)** — the Apr 15 live incident where `The.French.Chef.Season.05.03of20.Queen.of.Sheba.Cake` was returned as a top-seeded candidate for a "The Boys" S05E03 query is now defended against by a post-fetch token filter (`filter_results_by_show_title` in `src/search.rs`, shipped v3.1.0 commit `33c5e5e`). 7 regression tests including the Apr 15 live fixture. **Root cause still unknown** — Torrentio's IMDb-ID-routed endpoint shouldn't return cross-show data, but it did. Left as a defense-in-depth open question: diagnose whether this is a Torrentio upstream bug, a TMDB multi-search mismatch resolving to the wrong IMDb ID, or a spela fallback path we didn't expect. Not blocking any user-facing flow.

- [ ] **Real release-name corpus test for the ranker** — the resolution-tier tests use live fixtures, but we still lack a broad corpus of ~50 real Torrentio titles to catch regressions in tier 1-3 logic with realistic naming variation. Add `tests/ranker_corpus.rs` that loads the fixture and asserts canonical ordering. Should include every Apr 15 regression case as a pin.


- [ ] **Custom Cast Receiver — Google Cast SDK $5 registration.** Fredrik offered to pay; not yet done. Registration unlocks the Custom Receiver App ID needed by `static/cast-receiver.html` (Shaka Player + CAF v3). Benefits: (1) **native seeking** — Shaka does HLS seeking by segment index, the current server-side `/api/seek-restart` restart-the-transcode-from-new-ss approach becomes unnecessary; (2) **text-track handling** — can send WebVTT subtitles separately via Cast protocol instead of burning into video, eliminating the subtitle shift dance in `transcode.rs::shift_srt`; (3) **loading state UI** — replaces the blue-cast-icon "Black Flash" between clips. Blocking step: register at `cast.google.com/publish`, pay $5, get Receiver App ID, set it in `~/.config/spela/config.toml`, switch `cast.rs::cast_url` to load the custom receiver instead of Default Media Receiver. Spec lives at `.claude/specs/custom-cast-receiver/`.

- [x] **Intro concat fix** (SHIPPED Apr 30, 2026 — awaiting live validation): four ffmpeg args added to the has_intro h264_nvenc branch in both `transcode()` and `transcode_hls()`. Original investigation note follows for context. **Validate by**: `mv ~/.config/spela/intro.mp4.disabled ~/.config/spela/intro.mp4` on Darwin, play any title, verify smooth playback past segment 5. Pre-commit verification recipe (per investigation note): `ssh darwin 'for i in 00000 00001 00004 00005 00006; do ffprobe -v error -select_streams v -show_frames -read_intervals "%+#1" ~/media/transcoded_hls/seg_${i}.ts | grep -E "key_frame|pict_type"; done'` — every segment's first frame must be `key_frame=1 pict_type=I`.

- [ ] ~~**Intro concat investigation**~~ — with `--no-intro`, HLS plays smoothly; WITH intro, Chromecast (CrKey 1.56) buffers indefinitely at ~segment 5 (30-36s into playback). **Hypothesis** (Apr 19 session): NVENC's scene-change detection fires an unscheduled IDR at the intro→main concat seam (t≈5s, a huge visual transition from the 5s intro clip to the movie opening). That desyncs NVENC's GOP cadence from the HLS segmenter's 6s clock. Segments 1-4 happen to land on keyframes by luck; by segment 5, accumulated cadence drift means either (a) the segment doesn't start with a keyframe, or (b) its actual duration disagrees with its EXTINF value by more than CrKey 1.56 Shaka Player's tolerance. The concat seam itself is inside segment 0 (0-6s = intro 0-5 + main 5-6), not at the segment-5 boundary — so the failure is a *downstream consequence* of the seam, not the seam directly. **Proposed fix** — add four args to both `transcode()` and `transcode_hls()` in `src/transcode.rs` (the filter_complex + nvenc branch when `has_intro`): `-g 180 -keyint_min 180 -sc_threshold 0 -force_key_frames expr:gte(t,n_forced*6)`. Fixed 6s GOP at 30fps, disables NVENC scene-change IDR injection, guarantees an IDR at every 6s boundary regardless of encoder cadence — the canonical ffmpeg+HLS incantation. **Cheap pre-commit verification**: after a with-intro play, `ssh darwin 'for i in 00000 00001 00004 00005 00006; do ffprobe -v error -select_streams v -show_frames -read_intervals "%+#1" ~/media/transcoded_hls/seg_${i}.ts | grep -E "key_frame|pict_type"; done'` — every segment's first frame must be `key_frame=1 pict_type=I`. If segment 5's first frame is `pict_type=P`, that's hypothesis confirmed. **Not urgent**: `--no-intro` workaround is solid and voice-assistant paths already pass it by default for resume/seek plays. Prioritize below user-facing regressions.

### Infrastructure Hardening
- [ ] **Persist Chromecast DNS DNAT hijack to hemma Darwin overlay.** Currently live in Darwin's `/etc/iptables/rules.v4` (backed up to `rules.v4.bak-2026-04-15`), but not tracked in `~/dotfiles/hemma` — a fresh provision would lose it and `stream_host = "darwin.home"` would silently break again. Track the PREROUTING DNAT rules (one per Chromecast IP, TCP+UDP port 53 → `192.168.4.1:53`) as a managed fragment. See [CLAUDE.md](CLAUDE.md) § "Chromecast hardcoded Google DNS" and the [hemma overlay discipline directive in ~/dotfiles/home/dot_claude/CLAUDE.md](~/.claude/CLAUDE.md) § "Fresh provision is the litmus test". 🛡️

- [ ] **Passport.dmg — formal SPOF acceptance pending Gustav's Synology NAS revival** (May 20-21, 2026). The 1.8 TB corrupted-WD-recovery image on BOHR is documented DO-NOT-MOUNT per `~/dotfiles/docs/INCIDENT_REPORT_FERMI_FILESYSTEM_CORRUPTION_2026_01_29.md`. The earlier "needs 2 TB+ drive, spring 2026" deferred decision (`backup_ssd_analysis_2026_01_28.md`) has LAPSED; replaced by a person-conditional trigger: **Gustav's Synology NAS (~15 TB, currently unplugged)** per `~/Projects/sarpetorp/docs/MEDIA_STREAMING_PLAN.md` + `HOME_AUTOMATION_PLAN.md`. SHA-256 bit-rot canary recorded May 20 2026 (`bb9c33...`, 4h 33m run, 1.8 TiB sustained-read at ~115 MB/s) — see `~/dotfiles/docs/passport_dmg_sha256_2026-05-20.md` for the full canary doc + cross-references + two future-use cases (post-NAS-revival rsync verify + future bit-rot detection on BOHR copy). **Action when Gustav's NAS lives**: rsync Passport.dmg over SSH (~4h at gigabit, bypasses Apple's broken SMB client per `macos_usb_drive_regression_research_2026_02.md`), `shasum -a 256` the destination, verify it matches the canary, document the transfer in the canary doc. *Consult-before-shared-infra* (Gustav's NAS is his hardware) — coordinate with him on placement + access before transferring. 🛡️

### Post-Search Optimization
- [ ] Implement Chromecast Receiver UI loading state (spinner/progress) to replace the "Black Flash" transition between video clips. 🎉🏙️
- [ ] Refine year-aware and quality-aware result prioritisation in `src/search.rs` to better handle franchise sequels (e.g., 2025 vs 2026). 🕵️‍♂️
- [ ] **TVTime "next unwatched episode" integration for Ruby** — Ruby should know which episodes the user has actually watched before guessing from search ranking. Tonight's failure mode: when user said "play the Boys episode" with no season/episode, Ruby chose the latest available (S05E02 — unwatched) instead of the next unwatched (S05E01). Architecture sketched in `~/.claude/hooks/conversation_engine.py` near `SPELA_TOOL_DECLARATION`: new `tvtime_client.py` with `get_show_progress(show)` + `mark_watched(show, s, e)`, new Gemini tool `get_next_episode(show)` registered alongside `run_spela`, system-prompt rule "before run_spela play on a TV show, always call get_next_episode unless the user explicitly named the season/episode", read-through 1h cache invalidated on every successful spela play of a TV show. Auth: TVTime removed their public API ~2019; reverse-engineered sidecar (`https://beta.tvtime.com/sidecar?o=https://api.tvtime.com/v1/...`) is the only stable route. Trakt.tv has an official API and many TVTime users sync to it — prefer Trakt if available. Currently blocked on user finding working credentials (Apr 15 reset-password-hell). Cross-reference: dotfiles commit `57b2d92`.

### System Hardening
- [x] Increase Node.js heap memory limits by default for webtorrent-cli to prevent "Ghost Crashes" during large file verification. Implemented in `src/torrent.rs` with `--max-old-space-size=4096`.
- [x] Add robust stale WebTorrent worker detection to prevent orphan workers from inflating disk, RAM, and swap usage. See `OPERATIONS.md`.
- [x] Add a worker-only cleanup command/path that terminates WebTorrent/ffmpeg without deleting media or rewriting playback history. Implemented as local `spela kill-workers`.
- [x] Reconcile stale WebTorrent workers on server startup before accepting new playback.
- [x] Add systemd/cgroup containment for media workers (`MemoryHigh`, `MemoryMax`, task limits, and CPU quota/weight where practical). Implemented as `systemd/spela-resource-limits.conf`.
- [x] Tighten local-bypass completion markers so `.spela_done` is written only when expected torrent byte size and physical file bytes agree.
- [ ] Add an external watchdog/alert that reports orphan WebTorrent workers and resource pressure before the host becomes unhealthy.

### Subtitle source priority — prefer the torrent's BUNDLED sidecar .srt over OpenSubtitles
The VLC sub path (`handle_vlc_sub`) always fetches OpenSubtitles, which is often NOT synced to the specific rip → misaligned (T2 2026-08-14: OpenSubtitles srt drifted; the YIFY release's own sibling `.srt` was perfectly synced by construction). Fix: detect a sidecar `.srt` next to the video — either already on disk (`~/media/<release>/<name>.srt`) or by selecting+downloading its file index in the torrent (currently `only_files` selects the video index only). Priority order should be: embedded text track (already done) → bundled sidecar `.srt` (release-matched) → OpenSubtitles + alass align. Applies to the VLC path AND any future cast/browser sub path. Bonus: also honour multiple bundled langs (pick original-language per the never-a-dub rule).


